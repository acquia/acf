# wish button
- Toggles heart icon state from solid to outline
- Stores the entity id in localstorage (wishlist)

### TODO
 - Refactor the list management into a library so it can be shared with the wishlist component (to remove an item, for example)